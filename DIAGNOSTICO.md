# Diagnóstico del código de partida

Lea `clinicasegura/legado.py` entero antes de escribir una sola línea de
código nuevo. Llene una fila por principio. En la columna de evidencia
cite **archivo y línea** (por ejemplo `legado.py:38`); una fila sin
evidencia no cuenta.

Si cree que un principio **no** está violado, escriba la fila igual y
explique por qué en la columna de hallazgo.

| # | Principio | Hallazgo concreto | Evidencia (archivo:línea) | Qué cuesta si no se corrige |
|---|-----------|-------------------|---------------------------|------------------------------|
| 1 | Dividir y conquistar | `ServicioRecetas` hace todo: valida, calcula, habla por HTTP, escribe en base de datos, genera folios y exporta archivos. Es una sola clase con seis trabajos distintos. | legado.py:44-46 (docstring de la clase), legado.py:57 (`emitir`) | Cualquier cambio pequeño obliga a tocar la misma clase gigante; un solo error puede romper todo lo demás |
| 2 | Aumentar la cohesión | La misma clase mezcla lógica de negocio (calcular recargo), acceso a red (`_post`), acceso a base de datos y hasta escritura de archivos (`exportar`). No hay un tema único que la describa. | legado.py:72-76 (cálculo de recargo), legado.py:119 (`_post`), legado.py:169-173 (`exportar`) | Nadie puede entender "qué hace" la clase en una frase; cualquiera que la lea tiene que revisar todo el archivo |
| 3 | Reducir el acoplamiento | `CONFIG` es un diccionario global que leen y escriben varios métodos a la vez (acoplamiento común). Además, `reporte()` recibe el diccionario completo `paciente` para usar solo dos campos (acoplamiento de estampado). | legado.py:30-37 (`CONFIG`), legado.py:146-152 (`reporte`) | Cambiar un valor de `CONFIG` afecta partes del código sin avisar; cambiar el formato del paciente rompe `reporte()` aunque no tenga relación directa |
| 4 | Mantener alta la abstracción | `buscar_paciente()` devuelve el JSON crudo del proveedor externo tal cual llega. Todo el resto del sistema termina conociendo la forma exacta de ese JSON (`data.attributes.full_name`). | legado.py:137-144 (`buscar_paciente`), legado.py:149-152 (`reporte`, uso directo del JSON) | Si el proveedor cambia el formato de su respuesta, hay que perseguir y corregir el código en muchos lugares distintos |
| 5 | Aumentar la reusabilidad | El método `emitir()` mezcla reglas de negocio (calcular recargo) con detalles técnicos (armar el JSON de cada farmacia). No se puede reusar la regla de negocio sola en otro contexto. | legado.py:72-93 (recargo mezclado con las llamadas a `_post`) | Si otro sistema necesita solo la regla del recargo, tiene que copiar y pegar código en vez de reutilizarlo |
| 6 | Reusar lo existente | `validar_cedula()` reimplementa a mano una validación de formato que ya se puede hacer con herramientas estándar (expresiones regulares). | legado.py:154-167 (`validar_cedula`) | Ese código hecho a mano puede tener errores que una librería probada ya resolvió, y hay que mantenerlo para siempre |
| 7 | Diseñar para la flexibilidad | Agregar una farmacia nueva obliga a editar el método `emitir()` y agregarle un `elif` más. La variación de negocio está en el lugar equivocado. | legado.py:79-95 (cadena de `if/elif` por cadena de farmacia) | Cada farmacia nueva es un riesgo de romper las que ya funcionan, porque hay que tocar la misma función una y otra vez |
| 8 | Anticipar la obsolescencia | No hay evidencia de dependencias frágiles: el archivo solo usa librerías estándar de Python sin versión específica atada a un proveedor (json, os, random, sqlite3, urllib.request, datetime). No hay un principio violado aquí. | legado.py:16-20 (bloque de imports) | — |
| 9 | Diseñar para la portabilidad | El método `exportar()` tiene una ruta escrita directamente para Windows (`C:\ClinicaSegura\...`). No puede correr igual en Linux o Mac. | legado.py:169 (valor por defecto del parámetro `ruta`) | El programa se rompe apenas alguien lo corre en otro sistema operativo distinto a Windows |
| 10 | Diseñar para la testabilidad | La conexión a base de datos se crea dentro del constructor (no se puede sustituir). La fecha (`datetime.now()`) y el folio (`random.randint`) se generan directamente dentro de `emitir()`, sin poder controlarlos desde una prueba. | legado.py:50 (conexión sqlite3 en `__init__`), legado.py:66 (`datetime.now()`), legado.py:69 (`random.randint`) | Es imposible escribir una prueba automática confiable: cada corrida da una fecha y un folio distintos, y siempre necesita una base de datos real |
| 11 | Diseñar defensivamente | Los `assert` que validan los datos del formulario desaparecen si el programa corre con `python -O`. Además, `except Exception: pass` no muestra errores, y `while True` reintenta en bucle. | legado.py:62-63 (`assert`), legado.py:103-104 (`except Exception: pass`), legado.py:121,133-135 (`while True` y límite de un millón) | Un dato inválido puede pasar sin ser detectado en producción, un error real puede quedar completamente oculto, y una farmacia caída puede dejar el programa reintentando indefinidamente |